/* import { saveTask } from '../src/lib/firebase';

describe('saveTask', () => {
  it('Deberia de guardar ', () => saveTask('Hola').then((post) => {

    expect(post).toBe('String');
  }));
});

const loginFunction = jest.fn(email, password) => {
    if(email ==='aria@gmail.com' && password === 'pimienta123'){
        return Promise.resolve(navigateToMock)
    } else {
        return Promise.reject(new Error('error'))
    }
}
*/
